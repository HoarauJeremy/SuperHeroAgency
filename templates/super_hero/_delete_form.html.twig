<form method="post" action="{{ path('app_super_heros_delete', {'id': super_hero.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input
        type="hidden" name="_token" value="{{ csrf_token('delete' ~ super_hero.id) }}">
    <button class="px-2 py-1 text-white bg-red-600 border rounded shadow-md btn hover:bg-red-500">Delete</button>
</form>
