{% import "macro/bouton.twig" as btn %}

{{ form_start(form) }}
    {{ form_errors(form) }}
    
    {{ form_widget(form) }}
    {{ btn.modification(button_label|default('Enregistrer')) }}
{{ form_end(form) }}
